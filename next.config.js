/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: 'export',
  images: {
    // unoptimized: true
    domains: ['via.placeholder.com'],
  }
}

module.exports = nextConfig
